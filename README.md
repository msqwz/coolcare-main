<<<<<<< HEAD
<<<<<<< HEAD
# coolcare
=======
# CoolCare — Telegram Web App для мастеров кондиционеров
=======
# CoolCare PWA 🛠️
>>>>>>> 0dce058 (refactor: полная переделка в PWA (SMS auth, без Telegram))

Progressive Web App для мастеров по ремонту кондиционеров.

## 🚀 Возможности

- ✅ **PWA** — установка на телефон, работа offline
- ✅ **SMS аутентификация** — вход по номеру телефона
- ✅ **Заявки** — создание, редактирование, завершение
- ✅ **Клиенты** — база данных клиентов
- ✅ **Календарь** — планирование визитов
- ✅ **Маршруты** — оптимизация пути между заявками
- ✅ **Аналитика** — статистика работы

## 📱 Установка

### Локальная разработка

```bash
# Backend
cd backend
pip install -r requirements.txt
python main.py

# Frontend (в другом терминале)
cd frontend
npm install
npm run dev
```

### Docker

```bash
docker build -t coolcare-pwa .
docker run -p 8000:8000 coolcare-pwa
```

### Render.com

1. Подключите репозиторий
2. Build Command: `cd frontend && npm install && npm run build`
3. Start Command: `python app.py`
4. Добавьте переменные окружения

## 🔧 Переменные окружения

```env
API_BASE_URL=http://localhost:8000
WEB_APP_URL=http://localhost:5173
YANDEX_MAPS_API_KEY=your_key
JWT_SECRET=your_secret_key
JWT_EXPIRE_HOURS=24
SMS_SERVICE_API_KEY=your_sms_key
```

## 📡 API

### Аутентификация
- `POST /auth/send-code` — отправка SMS кода
- `POST /auth/verify` — проверка кода, получение токенов
- `POST /auth/refresh` — обновление токена
- `GET /auth/me` — информация о пользователе

### Заявки
- `GET /jobs` — список заявок
- `POST /jobs` — создать заявку
- `GET /jobs/{id}` — информация о заявке
- `PUT /jobs/{id}` — обновить заявку
- `DELETE /jobs/{id}` — удалить заявку
- `POST /jobs/{id}/complete` — завершить заявку

<<<<<<< HEAD
Для локального тестирования Web App без Telegram используйте режим демо (см. App.jsx).
>>>>>>> b7a70d6 (Q-1)
=======
### Клиенты
- `GET /customers` — список клиентов
- `POST /customers` — создать клиента
- `PUT /customers/{id}` — обновить клиента
- `DELETE /customers/{id}` — удалить клиента

### Аналитика
- `GET /analytics` — статистика
- `GET /route/optimize` — оптимизация маршрута

## 📦 Структура

```
Project-Qwen/
├── frontend/          # PWA приложение (React + Vite)
│   ├── src/
│   │   ├── App.jsx   # Главный компонент
│   │   ├── sw.js     # Service Worker
│   │   └── ...
│   ├── public/
│   │   └── manifest.json
│   └── index.html
├── backend/           # FastAPI API
│   ├── main.py       # API роуты
│   ├── auth.py       # Аутентификация
│   ├── models.py     # SQLAlchemy модели
│   ├── schemas.py    # Pydantic схемы
│   └── database.py   # SQLite БД
├── app.py            # Точка входа
├── Dockerfile
└── requirements.txt
```

## 🔐 Безопасность

- JWT токены для аутентификации
- Хэширование паролей (bcrypt)
- CORS для защиты API
- Валидация данных

## 📱 PWA Features

- Service Worker для offline режима
- Manifest для установки на устройство
- Push уведомления (заготовка)
- Адаптивный дизайн

## 🚀 Деплой

### VPS

```bash
# На сервере
git clone <repo>
cd Project-Qwen
pip install -r backend/requirements.txt
cd frontend && npm install && npm run build
cd ..
python app.py
```

### Render.com

Следуйте инструкции в README

## 📝 Лицензия

MIT
>>>>>>> 0dce058 (refactor: полная переделка в PWA (SMS auth, без Telegram))
